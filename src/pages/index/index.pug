extends _index
include includes/custom

block meta
    meta(charset='utf-8')
    meta(name='viewport'
        content='width-device-width'
        user-scalable='no'
        initional-scale='1.0'
        maximum-scale='1.0'
        minimum-scale='1.0')

block link

block title
    title #{htmlWebpackPlugin.options.title}

block colors & type
    .app-wrapper
        .emblem
            +PasteImg('emblem__img', 'Emblem', 'Toxin')
        include includes/Colors & Type

block form elements
    .app-wrapper
        .emblem
            +PasteImg('emblem__img', 'Emblem', 'Toxin')
        include includes/Form elements

block cards
    .app-wrapper
        .emblem
            +PasteImg('emblem__img', 'Emblem', 'Toxin')
        include includes/Cards

block headers & footers
    //.emblem
    //    img.emblem__img(src=require('../../img/Emblem.svg'))
    //include includes/Headers & Footers


